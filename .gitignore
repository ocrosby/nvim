tags
test.sh
.luarc.json
nvim

spell/

# Local development tracking file used by Claude Code sessions.
TODO.md

# Lazy.nvim lockfile — recommended to track per https://lazy.folke.io/usage/lockfile.
# Ignored here while the distribution is still settling; uncomment to track once stable.
lazy-lock.json

.DS_Store
